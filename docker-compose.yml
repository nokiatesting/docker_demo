version: '3'
services:
  demo:
    image: nokiatesting/docker_demo:mysql
    ports:
      - "5000:5000"
    depends_on:
      - mysql
  mysql:
    image: mysql:5
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - mydata:/var/lib/mysql
    networks:
      - testnet
